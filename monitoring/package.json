{
  "name": "monitoring",
  "scripts": {
    "monitor": "serverless invoke local --function monitor"
  },
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-sns": "^3.335.0",
    "aws-sdk": "^2.1381.0",
    "axios": "^1.4.0"
  }
}